# THIS FILE HAS BEEN GENERATED BY THE COMMAND `go:ci`; DO NOT EDIT;
name: go
on:
  push:
    branches:
      - develop
      - main
      - master
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      matrix:
        go: ['1.25']
        os: [macos-latest, ubuntu-latest]
    name: test
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ matrix.go }}
      - name: build
        run: go build ./...
      - name: test
        run: go test -race ./...

