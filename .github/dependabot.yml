# THIS FILE HAS BEEN GENERATED BY THE COMMAND `dependabot:config`;
# It is generated once and never overwritten: edit it freely.
version: 2
updates:
  # Go modules, including the tools pinned in the `tool` block of go.mod (golangci-lint, tk, ...).
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    groups:
      gomod:
        patterns:
          - "*"
    commit-message:
      prefix: chore
      include: scope

  # Actions used by the workflows in .github/workflows.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - "*"
    commit-message:
      prefix: chore
      include: scope

  # This repository ships a Dockerfile.
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
      include: scope

